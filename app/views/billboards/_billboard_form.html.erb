<h1><%= @billboard.id ? 'Edit Billboard' : 'Create Board' %></h1>
<%= link_to  "back to billboards", billboards_path %>
<hr />
<%= form_for [@billboard] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>


  <%= f.submit %>
<% end %>